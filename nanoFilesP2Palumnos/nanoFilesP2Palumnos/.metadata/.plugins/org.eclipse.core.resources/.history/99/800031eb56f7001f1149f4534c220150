package es.um.redes.nanoFiles.udp.server;

public record FileInfo(String name, long size, String hash) {
	
	// Vamos a crear una nueva clase, llamada FileInfo.
	// La funcionalidad de esta clase será la de almacenar tripletas de datos que contenga información 
	// sobre los ficheros subidos al directorio.
	// La tripleta consistirá en tres Strings, uno para el nombre del fichero, otro para el tamaño 
	// del mismo y un último para su código hash.
	// Como simplemente es una clase para guardar información, vamos a declararla como un tipo record.
	
}
